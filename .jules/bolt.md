## 2024-05-18 - Queue Component React.memo
**Learning:** `QueueTrack` and `LogEntry` components in React are often rendered in lists, and they take simple props. When their parent re-renders, they re-render.
**Action:** Use `React.memo` for list item components to prevent unnecessary re-renders.
